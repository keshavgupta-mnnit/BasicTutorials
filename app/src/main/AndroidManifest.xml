<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.keshav.basictutorials">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".lifecycle.FirstActivity" />
        <activity android:name=".lifecycle.SecondActivity" />
        <activity android:name="com.keshav.basictutorials.multithreading.threads.ThreadActivity" />
        <activity android:name="com.keshav.basictutorials.multithreading.async.AsyncActivity" />
        <activity android:name="com.keshav.basictutorials.multithreading.threadexecutor.ThreadExecutorActivity" />
        <activity android:name=".multithreading.asyncloader.AsyncLoaderActivity"/>
        <activity android:name=".rxjava.RxActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>